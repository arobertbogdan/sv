<div class="comment" style="margin-left: 50px;">
  <p>
    <%= comment.body %>
  </p>
  <p>created by <strong><%= comment.nickname %></strong></p>
  <p>submited <span><%= comment.old %></span> ago</p>
  <a id="<%= comment.id %>" class="reply">Reply</a>

  <%= render partial: "comments/comment", collection: comment.child.child_comments%>
</div>